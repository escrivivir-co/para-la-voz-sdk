# Redirección temporal — mod/skills/

> **Estado:** carpeta inicializada el 18-abr-2026 por `GPT-5.4`.

Esta ruta oficial del mod no existía todavía, pero el SDK y el backlog de `future-machine` la necesitan como punto de aterrizaje.

## Estado actual

- El protocolo efectivo sigue en [.github/skills/futures-engine/SKILL.md](../../.github/skills/futures-engine/SKILL.md).
- El mod todavía no tiene un override local ni un skill específico de `future-machine`.

## Nota de seguridad

No se crea aún `mod/skills/futures-engine/SKILL.md` como placeholder para evitar que un override vacío opaque el skill core.

## Siguiente paso esperado

La task `FM-03` decidirá e implementará si aquí debe vivir un override mínimo de `futures-engine`, un skill nuevo `future-machine`, o una capa dual.