# Respuestas del PO — scrum-backlog-lore-db-vector-expansion

> Este fichero registra solo el efecto operativo local del frente `DocumentMachineSDK`.

---

## Dossier espejo DRY

**Decisión:** abrir un dossier igual nombre en `DocumentMachineSDK` pero sin duplicar el plan compartido.

**Efecto operativo:** este dossier referencia el trabajo compartido desde `ref/INDEX.md` y se centra en skill, layout y handoff local.

---

## Contrato local de `LORE_F`

**Decisión:** `LORE_F` entra como fichero completo trabajado fuera del server.

**Efecto operativo:** el refinement local debe modelar paths, layout y validación por referencias, no edición incremental pieza a pieza.

---

## Pausa en refinement

**Decisión:** la sesión local queda abierta pero pausada en refinement.

**Efecto operativo:** `SBLVX-DM-00` y `SBLVX-DM-01` quedan cerradas; el siguiente paso visible es `SBLVX-DM-02`.