# Respuestas del usuario — future-machine-legislativa

> **Fecha:** 19-abr-2026
> **Registradas por:** GPT-5.4

## Punto 1 — Hay que traer la future-machine al nuevo ecosistema del mod

- **Contexto:** El usuario pide el mismo patrón de doble dossier para la `future-machine`, con una parte exportada a `main` y otra para usarla en `mod/legislativa`.
- **Respuesta del usuario (19-abr-2026):** quiere “el que la usamos en mod legislativa y podemos levantar los datos que tenemos ya”.
- **Efecto operativo:** Se crea `future-machine-legislativa` como instancia del contrato portable sobre el caso Feo.

## Punto 2 — Tiene que vivir sobre la inicialización nueva del lore exportado

- **Contexto:** El usuario aclara que la machine se usará cuando ya exista la inicialización y el export del lore actual.
- **Respuesta del usuario (19-abr-2026):** la machine debe poder arrancar sobre ese ecosistema nuevo, no sobre el workaround viejo.
- **Efecto operativo:** El dossier depende del export a `lore/`, del corpus canónico y del resto de capas ya separadas.

## Punto 3 — Portal y “empezar aquí” forman parte del cierre

- **Contexto:** El usuario exige que la machine conecte con `Portal`, `user-empieza-aqui` y superficies similares ya definidas.
- **Respuesta del usuario (19-abr-2026):** lo plantea explícitamente como “el cierre de todo el ciclo”.
- **Efecto operativo:** `future-machine-legislativa` integra `Portal`, `/user-empieza-aqui`, `/lore-status` y `Pipeline` como superficies de navegación del ciclo completo.